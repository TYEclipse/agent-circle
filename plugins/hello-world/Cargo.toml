[package]
name = "hello-world"
version = "0.1.0"
edition.workspace = true
description = "Agent Circle hello-world plugin — replies 'hello' → 'world'"
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
agent-circle-plugin = { path = "../../agent-circle-plugin" }
tracing = "0.1"
